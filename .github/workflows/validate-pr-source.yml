name: Validate PR Source Branch

on:
  pull_request:
    branches: [ main ]

jobs:
  check-source-branch:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check if PR is from develop
      run: |
        SOURCE_BRANCH="${{ github.head_ref }}"
        TARGET_BRANCH="${{ github.base_ref }}"
        
        echo "Source branch: $SOURCE_BRANCH"
        echo "Target branch: $TARGET_BRANCH"
        
        if [ "$TARGET_BRANCH" = "main" ] && [ "$SOURCE_BRANCH" != "develop" ]; then
          echo "❌ Error: Pull requests to 'main' must come from 'develop' branch only."
          echo "Current source branch: $SOURCE_BRANCH"
          exit 1
        fi
        
        echo "✅ PR source branch validation passed"